/**
 * Engine semantic version, kept as a string for trivial embedding in UIs and
 * diagnostic logs. Updated manually at release time (no automatic wiring yet).
 */
export const ENGINE_VERSION = '0.0.0'
